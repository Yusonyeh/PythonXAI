---

# 🧾 一、Streamlit 點餐機（像購物車）

```python
import streamlit as st
```

👉 使用 Streamlit 做網頁介面。

---

### ✅ Session State（記住資料）

```python
ss = st.session_state
```

Session State 就像「記憶盒子」，可以幫我們記住資料。

---

### 🧺 建立購物籃

```python
if "oders" not in ss:
    ss.oders = []
```

👉 如果還沒有訂單清單
👉 就建立一個空盒子 []

---

### ➕ 加入餐點

```python
ss.oders.append(input_food)
```

意思：把輸入的食物加入清單

---

### ❌ 刪除餐點

```python
ss.oders.pop(i)
```

👉 刪掉第 i 個食物

---

📌 重點：

✔ append() → 加
✔ pop() → 刪
✔ rerun() → 重新整理畫面

---

# ➕ 二、算術指定運算子

```python
a += b   # a = a + b
a -= b   # a = a - b
a *= b   # a = a * b
a /= b   # a = a / b
a //= b  # 整數除法
a %= b   # 取餘數
a **= b  # 次方
```

記法口訣：

👉 原本的 a 自己再做一次運算

例：

```python
a = 10
a += 3
```

結果：13

---

# 🔁 三、while 迴圈

```python
i = 0
while i < 5:
    print(i)
    i += 1
```

意思：

👉 只要 i < 5
👉 就一直做

結果：

0 1 2 3 4

---

### 🛑 break（強制停止）

```python
if i == 3:
    break
```

👉 遇到 3 就停止

---

# 🔁 四、for 迴圈 + break

```python
for i in range(1,6):
    if i == 3:
        break
```

👉 跑到 3 就停

---

# 🎲 五、random 隨機數

```python
import random
```

---

### 🎯 randrange

```python
random.randrange(6)     # 0~5
random.randrange(1,11)  # 1~10
random.randrange(0,10,2) # 0,2,4,6,8
```

---

### 🎯 randint

```python
random.randint(1,5)   # 1~5
```

---

### 📊 統計出現次數

```python
count[r-1] += 1
```

意思：

👉 對應的盒子 +1 次

---

# 🎮 六、猜數字遊戲（文字版）

電腦想一個數字

```python
target = random.randint(0,100)
```

玩家一直猜：

👉 太小
👉 太大
👉 剛好

猜對就結束 🎉

---

# 🎮 七、猜數字遊戲（Streamlit版）

使用 Session State 記住：

```python
ss.target
ss.min_val
ss.max_val
```

每猜一次：

👉 更新範圍
👉 顯示提示
👉 猜對 → 氣球 🎈

---

# 📦 八、字典 Dictionary

字典像：

👉 物品：數值
👉 鍵：值

```python
D2 = {"A":90, "B":80}
```

---

### 取資料

```python
D2["A"]
```

結果：90

---

### 走訪字典

```python
for key in D3:
```

---

### 看全部 key

```python
D3.keys()
```

### 看全部 value

```python
D3.values()
```

### 一次看兩個

```python
D3.items()
```

---

### 新增 / 修改

```python
D3["f"] = 6
D3["a"] = 10
```

---

### 刪除

```python
D3.pop("c")
```

---

# 📏 九、字典長度

```python
len(D3)
```

👉 有幾組資料

---

# 🔍 十、in 運算子（有沒有）

```python
"d" in D3
4 in [1,2,3,4]
```

👉 有 → True
👉 沒有 → False

---

# 📊 十一、各科成績平均

```python
scores = {"國文":[], "英文":[], ...}
```

每科加入 10 個隨機分數

再算：

```python
avg = total / 10
```

---
